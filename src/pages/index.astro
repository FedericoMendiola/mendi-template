---
import BaseLayout from '../layouts/BaseLayout.astro';
import demoData from '../data/leads/demo.json';
import type { SiteData } from '../types/site';

const data = demoData as SiteData;
---

<BaseLayout data={data} />